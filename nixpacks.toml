[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip']

[phases.build]
cmds = ['pip install -r backend/requirements.txt --break-system-packages']

[start]
cmd = 'cd backend && uvicorn app.main:app --host 0.0.0.0 --port '
