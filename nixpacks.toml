[phases.setup]
nixPkgs = ['python311']

[phases.build]
cmds = ['python3.11 -m venv /app/venv', '/app/venv/bin/pip install -r backend/requirements.txt']

[start]
cmd = 'cd backend && /app/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port '
